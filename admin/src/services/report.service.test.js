const {generate} = require('./report.service')

describe("report service", () => {
    describe("generate report", () => {
        test("should get user investments", async () => {})
        test("should get user holdings", async () => {})
        test("should correctly calculate user holdings", async () => {})
        test("should return report as csv", async () => {})
        test("each report line should contain correct details", async () => {})
    })
})